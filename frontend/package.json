{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "es6-promise": "^4.1.0",
    "fetch-jsonp": "^1.0.6",
    "isomorphic-fetch": "^2.2.1",
    "lodash-form-collector": "0.0.3",
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build  && rm -rf ../backend/public && cp -rf build ../backend/public ",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
